// src/pages/Profile.tsx
import React from "react";

const Profile: React.FC = () => {
  return (
    <div className="container">
      <h2>Profile / Settings</h2>
      <p>Select avatar, toggle dark/light mode, etc.</p>
    </div>
  );
};

export default Profile;
